import MessagesImp from "../models/Messages";

const Messages: MessagesImp = {
  errors: {
    INTERNAL_ERROR_SERVER: 'Internal Error Server',
    INTERNAL_ERROR_SERVER_MESSAGE: 'Something is wrong!'
  }
};

export default Messages;
